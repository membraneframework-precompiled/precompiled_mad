version: 2.1
orbs:
  elixir: membraneframework/elixir@dev:5db9d1338748dad57d9bbd342e980a6f2c76f712

workflows:
  precompile:
    jobs:
      - elixir/precompile_for_macos_intel: &parameters
          package-name: mad
          expected-version: << pipeline.git.tag >>
          filters:
            tags:
              only: /v.*/
      - elixir/precompile_for_macos_m1:
          <<: *parameters
      - elixir/precompile_for_linux:
          <<: *parameters
      - elixir/publish_precompiled:
          requires:
            - elixir/precompile_for_macos_m1
            - elixir/precompile_for_macos_intel
            - elixir/precompile_for_linux
          version: << pipeline.git.tag >>
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /v.*/
